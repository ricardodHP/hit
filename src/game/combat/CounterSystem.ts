import { COMBAT_CONFIG } from '../data/combatConfig';
export class CounterSystem { private readyMs=0; open(): void { this.readyMs=COMBAT_CONFIG.counterWindowMs; } update(deltaMs: number): void { this.readyMs=Math.max(0,this.readyMs-deltaMs); } consume(): boolean { if(this.readyMs<=0) return false; this.readyMs=0; return true; } get isReady(): boolean { return this.readyMs>0; } get remainingMs(): number { return this.readyMs; } }
